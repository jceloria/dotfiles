# -------------------------------------------------------------------------------------------------------------------- #
# mkpatch: Make a patch
function mkpatch() {
    [[ $# -ne 2 ]] && { echo 'Usage: mkpatch <old> <new>'; return 1; }

    diff -uNawbB ${OLD:=$1} ${NEW:=$2}
}
export -f mkpatch

# -------------------------------------------------------------------------------------------------------------------- #
