# -------------------------------------------------------------------------------------------------------------------- #
# inArray: Test for a string in an array
function inArray() {
  local e
  for e in "${@:2}"; do [[ "$e" = "$1" ]] && return 0; done; return 1;
}
export -f inArray

# -------------------------------------------------------------------------------------------------------------------- #
